# Quick Start Guide - Arriva Soft Frontend

## ⚡ Get Started in 3 Minutes

### Step 1: Install Dependencies

Open PowerShell and run:

`npm installcd c:\Drive_B\Arriva-App\Arriva-App-Frontend-Typescript`powershell

````



























































































































































































































**Happy Coding!** 🚀4. Check TypeScript errors in VS Code3. Review the documentation files2. Check browser console (F12)1. Check error messages in terminal## 📞 Need Help?---- **Lucide Icons**: https://lucide.dev/- **Tailwind Docs**: https://tailwindcss.com/docs- **TypeScript Docs**: https://www.typescriptlang.org/- **React Docs**: https://react.dev/- **Vite Docs**: https://vitejs.dev/## 🔗 Useful Resources---- States: `"hover:bg-blue-600 focus:ring-2"`- Responsive: `"text-sm md:text-lg lg:text-xl"`- Chain utilities: `"flex items-center gap-4 p-8"`- Use `className` for styling### 4. Tailwind ClassesBrowse icons: https://lucide.dev/icons/```<YourIcon className="w-6 h-6 text-blue-500" />import { YourIcon } from 'lucide-react';```typescriptUsing Lucide React:### 3. Adding Icons```export default MyComponent;);  </div>    <h2>{title}</h2>  <div className="p-4">const MyComponent: React.FC<MyComponentProps> = ({ title }) => (}  title: string;interface MyComponentProps {import React from 'react';```typescriptCreate new components in `src/components/`:### 2. Component Development- Tailwind CSS IntelliSense- Prettier- ESLintInstall recommended extensions for better DX:### 1. Use VS Code Extensions## 💡 Pro Tips---5. ✅ **Deploy**: Build and upload to hosting4. ✅ **Style**: Modify Tailwind classes in components3. ✅ **Customize**: Edit `src/data.ts` with your content2. ✅ **Review**: `ARCHITECTURE.md` to understand structure1. ✅ **Read**: `README.md` for full documentation## 🎯 Next Steps---```npm run preview```powershell**Preview production build locally:**Output will be in the `/dist` folder. Upload these files to your web host.```npm run build```powershellWhen ready to deploy:## 📦 Production Build---- Mac: `Cmd + Shift + R`- Windows: `Ctrl + F5`Hard refresh:### Browser Not Updating```npm install```powershellMake sure dependencies are installed:### TypeScript Errors```npm installRemove-Item package-lock.jsonRemove-Item -Recurse -Force node_modules```powershellClear and reinstall:### Dependencies Issues```npm run dev -- --port 3000```powershellIf port 5173 is taken:### Port Already in Use## 🐛 Troubleshooting---```npm run lint# Run linternpm run preview# Preview production buildnpm run build# Build for productionnpm run dev# Start development server```powershell## 🛠️ Available Commands---| Colors/theme | `tailwind.config.js` | Theme object || Global styles | `src/index.css` | CSS rules || Component UI | `src/components/*.tsx` | Individual files || Event banners | `src/data.ts` | `EVENT_BANNERS` || Footer links | `src/data.ts` | `FOOTER_LINKS` || Services shown | `src/data.ts` | `SERVICES_DATA` || Hero carousel | `src/data.ts` | `HERO_SLIDES` || Navigation menu | `src/data.ts` | `NAV_LINKS` ||---|---|---|| What to Change | File to Edit | Array/Object Name |## 🎨 Customization Quick Reference---3. Change `isActive: true/false`2. Find `EVENT_BANNERS` array1. Open `src/data.ts`### Toggle Event Banner3. Change headlines, images, stats, etc.2. Edit `HERO_SLIDES` array1. Open `src/data.ts`### Update Hero Slides3. Add/remove/modify links as needed2. Edit `NAV_LINKS` array1. Open `src/data.ts`### Change Navigation Links3. Save and see it appear immediately!   ```   }     href: '#'     description: 'Description here',     title: 'Your Service',     icon: YourIcon,   {   ```typescript2. Add new item to `SERVICES_DATA` array:1. Open `src/data.ts`### Add a New Service## 📝 Common Tasks---4. Watch the browser update automatically!3. Save the file2. Change a headline in `HERO_SLIDES`1. Open `src/data.ts`**Try it:**Any changes you make to the code will **instantly** appear in the browser.## 🔥 Hot Reloading---**That's it!** Your app is now running! 🎉Click the link or go to: `http://localhost:5173`### Step 3: Open in Browser```➜  Network: use --host to expose➜  Local:   http://localhost:5173/VITE v5.0.8  ready in 500 ms```**Output**: You'll see something like:```npm run dev```powershell### Step 2: Start Development Server**Wait time**: ~2-3 minutes (first time only)
````
