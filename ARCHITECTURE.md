# Arriva Soft - Project Architecture

## Component Hierarchy

````
App.tsx (Root Component)
│
├── EventBannerComponent (Conditional)






































































































































































































6. **Minimal Re-renders**: State localized to components5. **Component Memoization**: useCallback for handlers4. **CSS Purging**: Tailwind removes unused classes3. **Tree Shaking**: Unused code removed in production2. **Code Splitting**: Vite handles automatically1. **Lazy Loading**: Images use loading="lazy"## Performance Optimizations- Copyright & legal links- Link sections- Social media icons- Multi-column layout (2 → 5 columns)### Footer- Responsive columns (1 → 2 → 3)- Hover effects (lift + shadow)- Icon-based cards- 8-item grid (3-3-2 on desktop)### ServicesGrid- Image with stats overlay- Responsive grid layout- Manual slide controls- Auto-rotating carousel### Hero- Smooth animations- Mobile: Hamburger menu with accordion- Desktop: Mega menu with hover dropdowns- Fixed positioning (z-50)### Header## Key Features by Component```2xl: (≥ 1536px)    → Large desktopsxl: (≥ 1280px)     → Desktopslg: (≥ 1024px)     → Laptopsmd: (≥ 768px)      → Tabletssm: (≥ 640px)      → Large phonesDefault (< 640px)  → Mobile phonesMobile First Approach:```## Responsive Breakpoints```└── Output to /dist folder├── Asset optimization├── Code minification├── CSS optimization (PurgeCSS via Tailwind)├── Vite production build├── TypeScript compilation (tsc)Production Build (npm run build)└── Serves at http://localhost:5173├── Tailwind JIT compilation├── TypeScript checking├── Hot Module Replacement (HMR) enabled├── Vite Dev Server startsDevelopment (npm run dev)```## Build Process```└── Animations (transition, duration, transform)├── State variants (hover:, focus:, group-hover:)├── Responsive variants (sm:, md:, lg:)├── Utility classes for layout, spacing, colorsComponent Styles (Inline with Tailwind)└── Custom scrollbar styles├── Smooth scrolling├── Reset styles (*, body, #root)├── CSS custom properties (:root)├── Tailwind directives (@tailwind base, components, utilities)Global Styles (index.css)```## Styling Architecture```    └── Passed to: Hero component as prop    ├── Manual control: handleSlideChange callback    ├── Auto-increments: Every 8 seconds (useEffect)    ├── Initial value: 0└── currentSlideIndex (useState)│App.tsx (Parent Component)```## State Management```└── exports: Footer component├── imports: FOOTER_LINKS from data├── imports: Lucide icons├── imports: ReactFooter.tsx└── exports: ServicesGrid component├── imports: SERVICES_DATA from data├── imports: Lucide icons├── imports: ReactServicesGrid.tsx└── exports: Hero component├── imports: PrimaryButton, StatBox components├── imports: HeroContent type├── imports: HERO_SLIDES from data├── imports: Lucide icons├── imports: ReactHero.tsx└── exports: Header component├── imports: DropdownItem component├── imports: NavLink type├── imports: NAV_LINKS from data├── imports: Lucide icons├── imports: React, useStateHeader.tsx└── exports: App component├── imports: All components from ./components/├── imports: HERO_SLIDES, EVENT_BANNERS from data.ts├── imports: React hooks, Lucide iconsApp.tsx```## File Dependencies```└── FooterLinks → Used in Footer├── ServiceCard → Used in ServicesGrid├── EventBanner → Used in EventBannerComponent├── HeroContent → Used in Hero carousel├── NavLink → Used in Header navigation├── SubLink → Used in NavLink│types.ts (Type Definitions)```## Type System```└── FOOTER_LINKS → Footer Component│├── SERVICES_DATA → ServicesGrid Component││   └── Filtered by isActive flag├── EVENT_BANNERS → EventBannerComponent││   └── currentSlideIndex (state) → Auto-cycles every 8s├── HERO_SLIDES → Hero Component│├── NAV_LINKS → Header Component│data.ts (Single Source of Truth)```## Data Flow```    └── Company Column    ├── Industries Column    ├── Services Column    ├── Social Links    ├── Company Info & Logo└── Footer││       └── Client Logos (placeholder)│   └── Trusted By Section│   ││   │       └── Link│   │       ├── Description│   │       ├── Title│   │       ├── Icon│   │   └── ServiceCard (x8)│   ├── ServicesGrid│   ││   │   └── Slide Indicators│   │   ├── StatBox (x2)│   │   ├── PrimaryButton (CTA)│   │   ├── Headline & Subtext│   ├── Hero (Carousel)├── main (Content Area)││   └── Mobile Navigation (Hamburger Menu)│   │   └── DropdownItem (for each submenu item)│   ├── Desktop Navigation (Mega Menu)│   ├── Logo├── Header││   └── Shows active event banners from EVENT_BANNERS data```
````
